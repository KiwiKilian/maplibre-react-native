import ExampleSource from "!!raw-loader!@site/../examples/shared/src/examples/SymbolCircleLayer/Earthquakes";
import { ExampleCodeBlock } from "@site/src/components/ExampleCodeBlock/ExampleCodeBlock";

# Earthquakes

Renders earthquake data and clusters based on magnitude. Using the `onPress` prop of `ShapeSource` and the imperative `getClusterLeaves` method the details are retrieved for display.

<ExampleCodeBlock source={ExampleSource} />
